spring.application.name=tissenza-backend

# Database Configuration - Supabase PostgreSQL
spring.datasource.url=${DB_URL:jdbc:postgresql://db.ulruoludcmmphxmvayfh.supabase.co:5432/postgres}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:KDjtWWLtCP3olj1z}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Server Configuration
server.port=8080

# Cloudinary Configuration
cloudinary.cloud_name=${CLOUDINARY_CLOUD_NAME:di8fvoyfi}
cloudinary.api_key=${CLOUDINARY_API_KEY:587517946229931}
cloudinary.api_secret=${CLOUDINARY_API_SECRET:o-GDW6fFI3JdR2HMd6-5QHv7kvM}

# File Upload Configuration
app.upload.dir=${UPLOAD_DIR:uploads}

# JWT Configuration
tissenza.app.jwtSecret=${JWT_SECRET:NOUVEAU_JWT_SECRET}
tissenza.app.jwtExpirationMs=${JWT_EXPIRATION_MS:86400000}