spring.application.name=tissenza-backend

# Database Configuration - Local PostgreSQL
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/tissenza_dev}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:Papa1997}
spring.datasource.driver-class-name=org.postgresql.Driver

# H2 Console
spring.h2.console.enabled=false
spring.h2.console.path=/h2-console

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Server Configuration
server.port=8080

# Cloudinary Configuration
cloudinary.cloud_name=${CLOUDINARY_CLOUD_NAME:drrgj1x2a}
cloudinary.api_key=${CLOUDINARY_API_KEY:148125726987288}
cloudinary.api_secret=${CLOUDINARY_API_SECRET:<rWlZbbwBLCb5oPRQd3hrjX1FQwQw>}

# File Upload Configuration
app.upload.dir=${UPLOAD_DIR:uploads}

# JWT Configuration
tissenza.app.jwtSecret=${JWT_SECRET:Zk9rV1h4bXlSQU5ET00xMjM0NTY3ODkwQUJDREVGR0hJSktMTU5PUA==}
tissenza.app.jwtExpirationMs=${JWT_EXPIRATION_MS:86400000}
